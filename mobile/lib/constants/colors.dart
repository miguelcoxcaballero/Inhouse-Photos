import 'package:flutter/material.dart';

enum ImmichColorPreset { indigo, deepPurple, pink, red, orange, yellow, lime, green, cyan, slateGray }

// Inhouse brand copper. The lighter dark-mode tone keeps Material controls
// accessible on black surfaces while remaining inside the brand palette.
const Color immichBrandColorLight = Color(0xFFD97736);
const Color immichBrandColorDark = Color(0xFFF0A36D);
const Color whiteOpacity75 = Color.fromRGBO(255, 255, 255, 0.75);
const Color red400 = Color(0xFFEF5350);
const Color grey200 = Color(0xFFEEEEEE);
